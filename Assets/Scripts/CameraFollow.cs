using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static UnityEngine.GraphicsBuffer;

public class CameraFollow : MonoBehaviour
{
    /*public Transform player;
    public Quaternion offset;
    public Quaternion offsetX;
    public float angle;
    public float xAngle;

    public float speed;
    void Start()
    {
        angle = -90.0f;
    }

    void Update()
    {
        transform.position = player.transform.position + new Vector3(0, 8, -10);

        transform.rotation = Quaternion.Slerp(transform.rotation, player.transform.rotation * offset * offsetX, Time.deltaTime);
        offset = Quaternion.AngleAxis(angle, Vector3.up);
        offsetX = Quaternion.AngleAxis(xAngle, Vector3.right);
    }*/
}
